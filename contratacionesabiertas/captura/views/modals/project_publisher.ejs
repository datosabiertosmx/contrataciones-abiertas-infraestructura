<form id="update_project_publisher_form">
    <input type="text" name="id" value="<%= publisher.id %>" hidden>
    <p><label title='El nombre de la organización o departamento responsable de publicar estos datos.' >Nombre<!--Name--></label>
        <input name="name" class="form-control" value="<%= publisher.name %>" <%= publisher.name ? 'readonly' : '' %>></p>
    <p><label title='El esquema que contiene los identificadores únicos utilizados para identificar cada elemento.'>Esquema<!--Scheme--></label>
        <input name="scheme" class="form-control" value="<%= publisher.scheme %>" <%= publisher.scheme ? 'readonly' : '' %>></p>
    <p><label title='El identificador único para esta entidad bajo el esquema de identificación dado. Observe el uso de "uid" en lugar de "id".'>UID</label>
        <input name="uid" class="form-control" value="<%= publisher.uid %>" <%= publisher.uid ? 'readonly' : '' %>></p>
    <p><label title='Una URI para identificar al publicador.'>URI</label>
        <input type="url" name="uri" class="form-control" value="<%= publisher.uri %>" <%= publisher.uri ? 'readonly' : '' %>></p>   
</form>